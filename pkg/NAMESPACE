exportPattern("^[[:alpha:]]+")

useDynLib(DeconWK, .registration = TRUE, .fixes = ".DCWK_")

importFrom(kernlab, ipop)
importFrom(ks, Hpi)
importFrom(mvtnorm, dmvnorm)

importFrom(graphics, contour, plot, lines, points)
importFrom(stats, bw.SJ, rnorm, dnorm, sd, var, optimize, integrate)
importFrom(utils, packageDescription)

